<?xml version="1.0" encoding="UTF-8"?>
<CustomField xmlns="http://soap.sforce.com/2006/04/metadata">
    <fullName>Pay_To_Date_F__c</fullName>
    <description>IF ( ISBLANK( Pay_To_Date__c ), NULL,
DATE( VALUE(LEFT(Pay_To_Date__c,4)) ,VALUE(RIGHT(LEFT(Pay_To_Date__c,6),2)),VALUE(RIGHT(Pay_To_Date__c,2))) 
)</description>
    <formula>IF ( ISBLANK( Pay_To_Date__c ), NULL,
 DATEVALUE(Pay_To_Date__c ) 
)</formula>
    <formulaTreatBlanksAs>BlankAsZero</formulaTreatBlanksAs>
    <label>Pay To Date F</label>
    <required>false</required>
    <trackTrending>false</trackTrending>
    <type>Date</type>
</CustomField>
