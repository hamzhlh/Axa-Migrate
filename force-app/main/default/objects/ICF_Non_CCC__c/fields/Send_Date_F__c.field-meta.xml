<?xml version="1.0" encoding="UTF-8"?>
<CustomField xmlns="http://soap.sforce.com/2006/04/metadata">
    <fullName>Send_Date_F__c</fullName>
    <description>IF ( ISBLANK( Send_Date__c ), NULL,
DATE( VALUE(LEFT(Send_Date__c,4)) ,VALUE(RIGHT(LEFT(Send_Date__c,6),2)),VALUE(RIGHT(Send_Date__c,2))) 
)</description>
    <formula>IF ( ISBLANK( Send_Date__c ), NULL,
 DATEVALUE(Send_Date__c ) 
)</formula>
    <formulaTreatBlanksAs>BlankAsZero</formulaTreatBlanksAs>
    <label>Send Date F</label>
    <required>false</required>
    <trackTrending>false</trackTrending>
    <type>Date</type>
</CustomField>
